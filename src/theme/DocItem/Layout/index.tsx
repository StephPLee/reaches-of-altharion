import type { ReactNode } from "react";

import DocItemLayout from "@theme-original/DocItem/Layout";

export default function DocItemLayoutWrapper(props): ReactNode {
  return <DocItemLayout {...props} />;
}
